# RapidZipper アプリケーション製品仕様書

## 1. 概要
本ドキュメントは、Windows環境において高速なZIP圧縮および展開（解凍）を実現するデスクトップアプリケーション「RapidZipper」の仕様書です。Windows標準の圧縮・展開機能における処理速度の課題を解決するため、.NETのマルチスレッド機構を活用し、効率的なファイル処理を行うことを目的としています。
## 2. 動作環境
本アプリケーションの動作環境および開発環境は以下の通りです。
項目
要件
対象OS
Windows 11
フレームワーク
.NET 10.0 (Windows Forms アプリケーション)
開発ツール
Visual Studio 2026（UIデザイン）、Visual Studio Code（ロジック実装・AI監査）

## 3. 画面設計（ユーザーインターフェース）
直感的な操作を実現するため、ドラッグ＆ドロップを中心としたシンプルな1画面構成とします。
### 3.1. 配置コントロール一覧
コントロール名
型 (Type)
主要プロパティ設定
役割
Form1
Form
AllowDrop = True
Text = "RapidZipper"
メインウィンドウ。ファイル等のドロップを受け付ける。
DragDropPanel
Panel
Dock = Fill
AllowDrop = True
ユーザーがファイルやフォルダをドロップするための領域。
StatusLabel
Label
Text = "フォルダまたはZIPファイルをドロップしてください"
TextAlign = MiddleCenter
現在の処理ステータス（待機中、処理中、完了、エラー等）を表示する。
ProcessingBar
ProgressBar
Style = Blocks
処理中の進捗を視覚的に表現する。処理中はMarqueeスタイルに切り替わる。

## 4. 機能要件および処理フロー
### 4.1. ドラッグ＆ドロップ制御
DragEnter イベント: ドロップされたデータにファイルまたはフォルダ（FileDrop）が含まれているか判定し、含まれている場合はコピー効果（DragDropEffects.Copy）を設定します。
DragDrop イベント: ドロップされたファイルのパスを取得し、非同期で圧縮または展開処理を開始します。
### 4.2. 自動分岐ロジック
ドロップされたアイテムの属性に応じて、以下の通り処理を自動的に分岐します。
対象がフォルダの場合: ZIP圧縮処理を実行します。出力ファイル名は「対象フォルダ名.zip」となります。既存の同名ファイルがある場合は上書き（削除後に再生成）します。
対象がファイル、かつ拡張子が「.zip」の場合: ZIP展開処理を実行します。出力先は同一ディレクトリ内に生成される「対象ファイル名_extracted」フォルダとなります。
### 4.3. 非同期並列処理（パフォーマンス最適化）
大量のファイルや大容量データを処理する際、UIスレッドのフリーズ（応答なし状態）を防ぐため、以下の技術的アプローチを採用します。
async/awaitキーワードによる非同期メソッドの実装
Task.Runを用いたバックグラウンドスレッドへの処理の委譲
System.IO.Compression.ZipFileクラスの活用（内部的に最適化されたストリーム処理による高速化）
圧縮レベルにCompressionLevel.Fastestを指定し、処理速度を最優先する設定
## 5. 例外処理および安全対策
処理中にエラー（アクセス権限不足、ファイルロック、容量不足等）が発生した場合は、キャッチした例外メッセージをStatusLabelに表示し、アプリケーションの強制終了を防止します。
別スレッドからUIコントロールを操作する際のスレッドセーフな対応として、InvokeRequiredおよびInvokeメソッドを用いた安全なUI更新を徹底します。
